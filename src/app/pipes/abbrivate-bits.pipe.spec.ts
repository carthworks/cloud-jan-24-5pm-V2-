import { AbbrivateBitsPipe } from './abbrivate-bits.pipe';

describe('AbbrivateBitsPipe', () => {
  it('create an instance', () => {
    const pipe = new AbbrivateBitsPipe();
    expect(pipe).toBeTruthy();
  });
});
