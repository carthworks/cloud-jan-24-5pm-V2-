import { IszerolengthPipe } from './iszerolength.pipe';

describe('IszerolengthPipe', () => {
  it('create an instance', () => {
    const pipe = new IszerolengthPipe();
    expect(pipe).toBeTruthy();
  });
});
