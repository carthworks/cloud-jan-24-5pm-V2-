import { ProtoToNamePipe } from './proto-to-name.pipe';

describe('ProtoToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ProtoToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
